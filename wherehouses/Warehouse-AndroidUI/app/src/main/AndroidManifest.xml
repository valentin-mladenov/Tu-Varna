<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vale.warehouses">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.lease_request.CreateLeaseRequest"
            android:parentActivityName=".ui.login.LoginActivity" />
        <activity
            android:name=".ui.users.AddEditUserActivity"
            android:parentActivityName=".ui.users.UserListActivity" />
        <activity
            android:name=".ui.users.UserListActivity"
            android:parentActivityName=".ui.loggedin.LoggedInActivity" />
        <activity
            android:name=".ui.warehouses.WarehouseListActivity"
            android:parentActivityName=".ui.loggedin.LoggedInActivity" />
        <activity
            android:name=".ui.warehouses.AddEditWarehouseActivity"
            android:parentActivityName=".ui.warehouses.WarehouseListActivity" />
        <activity
            android:name=".ui.lease_contract.LeaseContractListActivity"
            android:parentActivityName=".ui.loggedin.LoggedInActivity" />
        <activity
            android:name=".ui.lease_contract.AddEditLeasingContractActivity"
            android:parentActivityName=".ui.lease_contract.LeaseContractListActivity" />
        <activity android:name=".ui.loggedin.LoggedInActivity" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>